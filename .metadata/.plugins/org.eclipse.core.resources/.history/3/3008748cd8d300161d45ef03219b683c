package step1;

public class TestPerson {
	/*
	 * 메인 메서드는 자바 프로그램의 시작점
	 */
	public static void main(String[] args) {
		// Person class를 이용해 객체 생성한다.
		Person p = new Person();
		p.name = "아이유";//변수(속성)에 이름을 저장한다.
		System.out.println("p.name");
		
		Person p2 = new Person();
		p2.name = "";
	}
}
